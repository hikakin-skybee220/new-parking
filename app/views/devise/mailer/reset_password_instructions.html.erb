<p><%= @resource.name %>様</p>

<p>パスワードを変更するには、以下のリンクをクリックしてください。このリンクの有効期限は1時間です。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>パスワードのリセットをリクエストしていない場合、このメールは無視してください。</p>
<p>このリンクをクリックして新しいパスワードを生成しない限り、あなたのパスワードは変更されません。</p>
